<div class="jdev-stage" aria-hidden="true">
  <!-- Desktop / tablet: layered AMV text behind portrait -->
  <div class="jdev-backdrop jdev-backdrop-full">
    <span class="jdev-text jdev-r">JDev</span>
    <span class="jdev-text jdev-b">JDev</span>
    <span class="jdev-text jdev-main">JDev</span>
  </div>

  <!-- Mobile: bold letters flanking the portrait so they stay readable -->
  <div class="jdev-flank">
    <span class="jdev-flank-letter jdev-flank-j">J</span>
    <span class="jdev-flank-letter jdev-flank-dev">Dev</span>
  </div>
</div>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap");

  .jdev-stage {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }

  /* Full watermark behind photo */
  .jdev-backdrop-full {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 160%;
    height: 140%;
    transform: translate(-50%, -50%);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .jdev-text {
    position: absolute;
    font-family: "Bebas Neue", Impact, sans-serif;
    font-size: clamp(6rem, 20vw, 14rem);
    font-weight: 400;
    line-height: 0.85;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    white-space: nowrap;
    transform: skewX(-10deg) rotate(-2deg);
    user-select: none;
  }

  .jdev-main {
    color: transparent;
    -webkit-text-stroke: 3px oklch(0.205 0 0);
    animation: jdev-pulse 4s ease-in-out infinite;
    z-index: 3;
    opacity: 0.45;
  }

  :global(.dark) .jdev-main {
    -webkit-text-stroke: 3px oklch(0.97 0 0);
    opacity: 0.5;
  }

  .jdev-r {
    color: oklch(0.56 0.25 25 / 0.65);
    -webkit-text-stroke: 2px oklch(0.56 0.25 25 / 0.85);
    animation: jdev-drift-r 5s ease-in-out infinite;
    z-index: 1;
  }

  .jdev-b {
    color: oklch(0.65 0.2 295 / 0.65);
    -webkit-text-stroke: 2px oklch(0.65 0.2 295 / 0.85);
    animation: jdev-drift-b 5s ease-in-out infinite;
    z-index: 2;
  }

  /* Side letters on mobile */
  .jdev-flank {
    display: none;
  }

  @media (max-width: 639px) {
    .jdev-backdrop-full .jdev-text {
      font-size: clamp(5.5rem, 34vw, 9rem);
      letter-spacing: 0.12em;
    }

    .jdev-main {
      opacity: 0.28;
    }

    .jdev-flank {
      display: flex;
      position: absolute;
      inset: 0;
      align-items: center;
      justify-content: space-between;
      padding: 0 0.15rem;
      z-index: 1;
    }

    .jdev-flank-letter {
      font-family: "Bebas Neue", Impact, sans-serif;
      font-size: clamp(4.5rem, 22vw, 6.5rem);
      line-height: 1;
      text-transform: uppercase;
      -webkit-text-stroke: 2px oklch(0.205 0 0);
      color: transparent;
    }

    .jdev-flank-j {
      margin-right: auto;
      margin-left: -0.25rem;
      transform: skewX(-8deg);
      animation: flank-drift 4s ease-in-out infinite;
      text-shadow: 4px 0 0 oklch(0.56 0.25 25 / 0.5), -4px 0 0 oklch(0.65 0.2 295 / 0.4);
    }

    .jdev-flank-dev {
      margin-left: auto;
      margin-right: -0.5rem;
      transform: skewX(-8deg) translateX(1.25rem);
      animation: flank-drift-dev 4s ease-in-out infinite;
      animation-delay: 0.5s;
      text-shadow: -4px 0 0 oklch(0.56 0.25 25 / 0.5), 4px 0 0 oklch(0.65 0.2 295 / 0.4);
    }

    :global(.dark) .jdev-flank-letter {
      -webkit-text-stroke: 2px oklch(0.92 0 0);
      opacity: 0.85;
    }
  }

  @media (min-width: 640px) {
    .jdev-backdrop-full {
      width: 180%;
    }

    .jdev-text {
      font-size: clamp(7rem, 18vw, 14rem);
    }
  }

  @keyframes jdev-drift-r {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) translate(-8px, 2px);
      opacity: 0.75;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) translate(-16px, -3px);
      opacity: 1;
    }
  }

  @keyframes jdev-drift-b {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) translate(8px, -2px);
      opacity: 0.75;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) translate(16px, 3px);
      opacity: 1;
    }
  }

  @keyframes jdev-pulse {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) scale(1);
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) scale(1.03);
    }
  }

  @keyframes flank-drift {
    0%,
    100% {
      transform: skewX(-8deg) translateY(0);
      opacity: 0.7;
    }
    50% {
      transform: skewX(-8deg) translateY(-4px);
      opacity: 1;
    }
  }

  @keyframes flank-drift-dev {
    0%,
    100% {
      transform: skewX(-8deg) translateX(1.25rem) translateY(0);
      opacity: 0.7;
    }
    50% {
      transform: skewX(-8deg) translateX(1.25rem) translateY(-4px);
      opacity: 1;
    }
  }
</style>
