<div class="jdev-backdrop" aria-hidden="true">
  <span class="jdev-text jdev-r">JDev</span>
  <span class="jdev-text jdev-b">JDev</span>
  <span class="jdev-text jdev-main">JDev</span>
</div>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap");

  .jdev-backdrop {
    position: absolute;
    inset: -4rem -6rem;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
  }

  .jdev-text {
    position: absolute;
    font-family: "Bebas Neue", Impact, sans-serif;
    font-size: clamp(7rem, 22vw, 14rem);
    font-weight: 400;
    line-height: 0.85;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    white-space: nowrap;
    transform: skewX(-10deg) rotate(-2deg);
    user-select: none;
  }

  .jdev-main {
    color: transparent;
    -webkit-text-stroke: 3px oklch(0.205 0 0);
    text-stroke: 3px oklch(0.205 0 0);
    animation: jdev-pulse 4s ease-in-out infinite;
    z-index: 3;
  }

  :global(.dark) .jdev-main {
    -webkit-text-stroke: 3px oklch(0.97 0 0);
    text-stroke: 3px oklch(0.97 0 0);
  }

  .jdev-r {
    color: oklch(0.56 0.25 25 / 0.55);
    -webkit-text-stroke: 2px oklch(0.56 0.25 25 / 0.7);
    text-stroke: 2px oklch(0.56 0.25 25 / 0.7);
    animation: jdev-drift-r 5s ease-in-out infinite;
    z-index: 1;
  }

  .jdev-b {
    color: oklch(0.65 0.2 295 / 0.55);
    -webkit-text-stroke: 2px oklch(0.65 0.2 295 / 0.7);
    text-stroke: 2px oklch(0.65 0.2 295 / 0.7);
    animation: jdev-drift-b 5s ease-in-out infinite;
    z-index: 2;
  }

  @keyframes jdev-drift-r {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) translate(-6px, 2px);
      opacity: 0.7;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) translate(-14px, -2px);
      opacity: 1;
    }
  }

  @keyframes jdev-drift-b {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) translate(6px, -2px);
      opacity: 0.7;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) translate(14px, 2px);
      opacity: 1;
    }
  }

  @keyframes jdev-pulse {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) scale(1);
      opacity: 0.35;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) scale(1.02);
      opacity: 0.55;
    }
  }
</style>
