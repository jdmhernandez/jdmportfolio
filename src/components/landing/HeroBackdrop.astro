<div class="jdev-stage" aria-hidden="true">
  <div class="jdev-backdrop jdev-backdrop-full">
    <span class="jdev-text jdev-r">JDev</span>
    <span class="jdev-text jdev-b">JDev</span>
    <span class="jdev-text jdev-main">JDev</span>
  </div>

  <div class="jdev-flank">
    <span class="jdev-flank-letter jdev-flank-j">J</span>
    <span class="jdev-flank-letter jdev-flank-dev">Dev</span>
  </div>
</div>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap");

  .jdev-stage {
    position: absolute;
    inset: -0.5rem -1.75rem;
    pointer-events: none;
    z-index: 0;
  }

  .jdev-backdrop-full {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 200%;
    height: 140%;
    transform: translate(-48%, -50%);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
  }

  .jdev-text {
    position: absolute;
    font-family: "Bebas Neue", Impact, sans-serif;
    font-size: clamp(6rem, 20vw, 14rem);
    font-weight: 400;
    line-height: 0.85;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    white-space: nowrap;
    transform: skewX(-10deg) rotate(-2deg);
    user-select: none;
  }

  .jdev-main {
    color: transparent;
    -webkit-text-stroke: 3px oklch(0.205 0 0);
    animation: jdev-pulse 4s ease-in-out infinite;
    z-index: 3;
    opacity: 0.4;
  }

  :global(.dark) .jdev-main {
    -webkit-text-stroke: 3px oklch(0.97 0 0);
    opacity: 0.45;
  }

  .jdev-r {
    color: oklch(0.56 0.25 25 / 0.6);
    -webkit-text-stroke: 2px oklch(0.56 0.25 25 / 0.8);
    animation: jdev-drift-r 5s ease-in-out infinite;
    z-index: 1;
  }

  .jdev-b {
    color: oklch(0.65 0.2 295 / 0.6);
    -webkit-text-stroke: 2px oklch(0.65 0.2 295 / 0.8);
    animation: jdev-drift-b 5s ease-in-out infinite;
    z-index: 2;
  }

  .jdev-flank {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .jdev-flank-letter {
    position: absolute;
    top: 50%;
    font-family: "Bebas Neue", Impact, sans-serif;
    line-height: 1;
    text-transform: uppercase;
    transform: skewX(-8deg) translateY(-50%);
    -webkit-text-stroke: 2px oklch(0.205 0 0);
    color: transparent;
    animation: flank-drift 4s ease-in-out infinite;
  }

  .jdev-flank-j {
    left: -0.15rem;
    font-size: clamp(4.5rem, 22vw, 6.5rem);
    text-shadow: 4px 0 0 oklch(0.56 0.25 25 / 0.5), -4px 0 0 oklch(0.65 0.2 295 / 0.4);
  }

  .jdev-flank-dev {
    right: -1.35rem;
    font-size: clamp(4.5rem, 22vw, 6.5rem);
    animation-delay: 0.5s;
    text-shadow: -4px 0 0 oklch(0.56 0.25 25 / 0.5), 4px 0 0 oklch(0.65 0.2 295 / 0.4);
  }

  :global(.dark) .jdev-flank-letter {
    -webkit-text-stroke: 2px oklch(0.92 0 0);
    opacity: 0.9;
  }

  @media (min-width: 640px) {
    .jdev-stage {
      inset: -1rem -3.5rem;
    }

    .jdev-backdrop-full {
      opacity: 1;
      width: 220%;
      transform: translate(-46%, -50%);
    }

    .jdev-text {
      font-size: clamp(7rem, 16vw, 14rem);
      letter-spacing: 0.1em;
    }

    .jdev-flank-j {
      left: -0.5rem;
      font-size: clamp(5.5rem, 10vw, 9rem);
    }

    .jdev-flank-dev {
      right: -2.75rem;
      font-size: clamp(5.5rem, 10vw, 9rem);
    }
  }

  @media (min-width: 1024px) {
    .jdev-stage {
      inset: -1.5rem -4.5rem;
    }

    .jdev-flank-j {
      left: -0.75rem;
      font-size: clamp(6rem, 8vw, 10rem);
    }

    .jdev-flank-dev {
      right: -3.5rem;
      font-size: clamp(6rem, 8vw, 10rem);
    }
  }

  @keyframes jdev-drift-r {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) translate(-8px, 2px);
      opacity: 0.75;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) translate(-16px, -3px);
      opacity: 1;
    }
  }

  @keyframes jdev-drift-b {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) translate(8px, -2px);
      opacity: 0.75;
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) translate(16px, 3px);
      opacity: 1;
    }
  }

  @keyframes jdev-pulse {
    0%,
    100% {
      transform: skewX(-10deg) rotate(-2deg) scale(1);
    }
    50% {
      transform: skewX(-10deg) rotate(-2deg) scale(1.03);
    }
  }

  @keyframes flank-drift {
    0%,
    100% {
      transform: skewX(-8deg) translateY(-50%);
      opacity: 0.75;
    }
    50% {
      transform: skewX(-8deg) translateY(calc(-50% - 4px));
      opacity: 1;
    }
  }
</style>
